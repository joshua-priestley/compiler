# a simple increment function definition and usage

# Output:
# 1
# 4

# Program:

begin
  int inc(int a, bool a1, int a2) is
    int g = 3 ;
    return a2 + 1
  end
  int x = 0 ;
  x = call inc(0, false && true, 2)
end
