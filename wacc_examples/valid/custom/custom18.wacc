# a simple increment function definition and usage

# Output:
# 1
# 4

# Program:
struct thing {
  int x;
  int y;
  char t;
};

struct thing2 {
  char t;
};

begin
  #thing y = new thing(1, 2, 'a');
  #int a = y.x;
  #y.y = a
  int x = 3
end
