# a simple increment function definition and usage

# Output:
# 1
# 4

# Program:
class thing(int z) {
  int x = z;
  int y = 10;

  int f() is
    return x
  end
};

begin
  int g(int x) is
    return 4
  end
  thing ting = new thing(3);
  int x = call g(3);
  int y = call ting.f()
end
