# a simple increment function definition and usage

# Output:
# 1
# 4

# Program:

begin
  int thing(pair(int, int) t) is
    int x = 3 ;
    int r = fst t ;
    return r
  end
  pair(int, int) p = newpair(1, 2) ;
  int x = call thing(p)
end
