image: gradle:4.7.0-jdk8-alpine

stages:
  - compile
  - test

compile:
  stage: compile
  script:
    - ./gradlew assemble

test:
  stage: test
  script:
    - ./gradlew test
  artifacts:
    reports:
      junit: build/test-results/TEST-*.xml